"""
Connect the FastAPI routes to `operations` for `{{cookiecutter.module_name}}`.

This file exports a router object for usage in FastAPI.

All route return values should be untyped or part of `serializers`
"""
from fastapi import APIRouter

router = APIRouter()
