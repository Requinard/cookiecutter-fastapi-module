"""
This file contains the basic model definitions for `{{cookiecutter.module_name}}`.

All classes here are pure and to be used in `operations`

They should never be published directly as a result of `routes`
"""
from pydantic import BaseModel
