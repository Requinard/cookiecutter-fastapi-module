"""
Handles the business logic and side effects for `{{cookiecutter.module_name}}`.

Operations are done on `models` or base types, never `serializers`

Operations can only be called from `commands`, `routes`, or `dependencies`

Operations can only return `models` or base types.
"""
